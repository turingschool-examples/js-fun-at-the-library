function createTitle(title) {
  return `The ${title}`
}

function buildMainCharacter() {
  
}

module.exports = {
  createTitle,
  buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}

// npm test test/book-test.js